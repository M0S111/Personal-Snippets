#include <iostream>
#include <string>

using namespace std;

int Sum(int,int,string);

int main()
{
	Sum(3,8,"+");
}

int Sum(int l,int u,string op)
{
	int sum;
	
	if (op == "+")
	{
		for (l; l < u; l++)
		{
			cout << "Idiot";
		}
	}
	
	
	
	return sum;
}